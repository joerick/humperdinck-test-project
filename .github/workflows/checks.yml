name: Test suite

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check formatting
        run: echo "Formatting looks good"
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: echo "All tests passed"
